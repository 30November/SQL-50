# Got to know

- derived table must have alias (Eg: FROM (...) dt) OR, use CTE
- (PostgreSQL, Oracle) must col to participate in GROUP BY if they are not aggregate func
- (MySQL) not robust on Group By cause ... by default it take the first value of each group records
